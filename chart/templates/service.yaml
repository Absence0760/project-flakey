apiVersion: v1
kind: Service
metadata:
  name: {{ include "flakey.fullname" . }}
  labels:
    {{- include "flakey.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "flakey.selectorLabels" . | nindent 4 }}
