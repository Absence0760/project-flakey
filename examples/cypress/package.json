{
  "name": "@flakeytesting/example-cypress",
  "private": true,
  "comments": {
    "deps": "To use published npm packages instead of local links, replace the link: entries with: \"@flakeytesting/reporter\": \"latest\", \"@flakeytesting/cypress-snapshots\": \"latest\""
  },
  "scripts": {
    "test:smoke": "SUITE=smoke cypress run",
    "test:sanity": "SUITE=sanity cypress run",
    "test:regression": "SUITE=regression cypress run",
    "test:all": "SUITE=smoke cypress run; SUITE=sanity cypress run; SUITE=regression cypress run",
    "open": "cypress open"
  },
  "devDependencies": {
    "@flakeytesting/reporter": "link:../../packages/reporter",
    "@flakeytesting/cypress-snapshots": "link:../../packages/cypress-snapshots",
    "cypress": "latest",
    "typescript": "latest"
  }
}
